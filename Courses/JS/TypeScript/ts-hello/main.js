/* let Keyword makes the varianle block scope based,
which elimiate errors when you go along the way in your project.

// better to use let when declaring a variable
// Declaring without initializing give it (any) type
// decalring an array with numbers e
*/

/*
// Declare a variable
function doSomething() {
    for (var i = 0; i < 5; i++) {
        console.log(i);
    }
    console.log('Finally: ' + i);
}
doSomething();
*/
